# Audit: eight carried-forward v2.3 patches vs. v3 upstream

Date: 2026-08-10

v2.3 needed 21 hand-patched generated Go files to work around spec defects,
protected only by a `.swagger-codegen-ignore` file the generator does not
read — one regeneration away from silent destruction. This audit re-checks
the eight candidates selected for carry-forward against `api/v3.upstream.yaml`
(never `api/swagger.yaml`, which is a build product) using
`scripts/audit_carried_patches.py`. A CONFIRMED verdict gets an
`api/overlay.yaml` entry; an ALREADY-FIXED verdict gets recorded here and
nothing else, so fixed-upstream defects are not carried forward as dead
corrections.

## A3 — address `{id}` (PUT) vs `{addressId}` (DELETE) param name

Not run through the audit script — codegen output does not exist yet
(`api_objects.go` is generated in Task 5, not before). Disposition decided
by inspecting the spec directly instead of by grepping generated code.

```
A3   address {id} vs {addressId} param name   DEFERRED   cosmetic only; both verbs
     generate working methods. Normalising would presume Q1's answer. Test row
     OA-N05 (update then immediately delete) detects the alternative at runtime.
```

Reasoning: the PUT and DELETE for objects/addresses live on different path
templates in `api/v3.upstream.yaml`, so openapi-generator emits two
independent methods regardless of whether their path parameters are named
`id` or `addressId` — there is no shared struct or interface that requires
the names to match. Normalising the name would silently assert that `{id}`
and `{addressId}` denote the same identifier space, which is exactly open
question Q1 (still unanswered with the API team). Codegen confirmation of
"both methods generate independently" is deferred to Task 5, once
`api_objects.go` exists and the `grep` the design spec proposes can actually
run. No overlay entry is written for A3.

## Audit script output (verbatim)

Command: `python3 scripts/audit_carried_patches.py`

```
A4                           Application GET oneOf discriminator        CONFIRMED     oneOf=True discriminator=False
A5                           ApplicationAuth.authEnabled required       CONFIRMED     required=['authEnabled']
A6:IPSecSharedSettingsCreate IPSecSharedSettingsCreate.p81ASN required  CONFIRMED     required=['p81ASN']
A6:IPSecRedundantTunnel      IPSecRedundantTunnel required list         ALREADY-FIXED required=[]
A6:NetworkTunnelIpsecRedundant NetworkTunnelIpsecRedundant required list  ALREADY-FIXED required=[]
A8                           peakBandwidth (not ...Mbps) property name  ALREADY-FIXED schemas=[]
A9:ASN                       ASN type/format                            ALREADY-FIXED type=integer format=None
A9:RemoteASN                 RemoteASN type/format                      ALREADY-FIXED type=integer format=None
A10                          EnhancedHealthCheckMeta required list      CONFIRMED     required=['networkId', 'tunnelName', 'tunnelId', 'regionId']
A11                          ObjectsServicesResponseObj.id present      CONFIRMED     properties=['description', 'name', 'protocols']

CONFIRMED: 5 / 10
```

## Verdicts and disposition

| Candidate | v2.3 defect | Verdict | Overlay entry |
|---|---|---|---|
| A3 | `{id}` vs `{addressId}` path-param naming on objects/addresses PUT vs DELETE | **DEFERRED** | none — see above |
| A4 | GET /v3/applications/{applicationId} 200 response is a 5-way `oneOf` with no `discriminator`, so the generated decoder cannot tell the five Application variants apart (P81-126016) | **CONFIRMED** | `A4-application-get-discriminator` |
| A5 | `ApplicationAuth.authEnabled` is declared `required`, but Read responses omit it entirely, so strict decoding fails on every application read (P81-126016) | **CONFIRMED** | `A5-application-auth-not-required` |
| A6:IPSecSharedSettingsCreate | `IPSecSharedSettingsCreate.p81ASN` declared `required`, but the field is server-assigned and absent on create (P81-124405) | **CONFIRMED** | `A6-ipsec-shared-settings-create-p81asn-not-required` |
| A6:IPSecRedundantTunnel | `IPSecRedundantTunnel` over-declared required list | **ALREADY-FIXED** | none — upstream `required` is now `[]` |
| A6:NetworkTunnelIpsecRedundant | `NetworkTunnelIpsecRedundant` over-declared required list | **ALREADY-FIXED** | none — upstream `required` is now `[]` |
| A8 | Schema property named `peakBandwidth` instead of the live API's `peakBandwidthMbps` | **ALREADY-FIXED** | none — no schema in v3 upstream declares `peakBandwidth`; the `t_rename_peak_bandwidth` transform described in the design spec is not registered |
| A9:ASN | `ASN` schema generated as an empty struct instead of a plain integer (P81-123406 / BUG-24) | **ALREADY-FIXED** | none — upstream already declares `type: integer` |
| A9:RemoteASN | `RemoteASN` schema generated as an empty struct instead of a plain integer (P81-123406 / BUG-24 twin) | **ALREADY-FIXED** | none — upstream already declares `type: integer` |
| A10 | `EnhancedHealthCheckMeta` over-declares `tunnelId`/`regionId` as required, but the public API omits both for tunnels that have not completed a health probe yet (P81-123406 / BUG-25) | **CONFIRMED** | `A10-enhanced-health-check-meta-not-required` |
| A11 | `ObjectsServicesResponseObj` is missing the `id` field the live API actually returns | **CONFIRMED** | `A11-objects-services-response-obj-id` |

Total: 5 CONFIRMED (A4, A5, A6:IPSecSharedSettingsCreate, A10, A11) → 5 overlay
entries. 4 ALREADY-FIXED sub-checks (A6:IPSecRedundantTunnel,
A6:NetworkTunnelIpsecRedundant, A9:ASN, A9:RemoteASN) plus A8 ALREADY-FIXED →
5 ALREADY-FIXED rows, 0 entries. A3 DEFERRED, 0 entries.

### Notable surprise

A8 and A9 were both expected (per the design spec's exact-code Step 3
snippets) to need an overlay entry — a `rename_peak_bandwidth` transform for
A8 and a `merge` for A9's `ASN`/`RemoteASN`. The audit shows upstream v3
already fixed both: no schema declares a `peakBandwidth` property, and
`ASN`/`RemoteASN` are already plain `type: integer`. No entry and no
transform function were added for either; carrying them forward would have
been dead corrections for defects that no longer exist.
